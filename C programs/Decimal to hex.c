//3.	Write a C code to print hexadecimal equivalent (base 16) of a decimal number.
#include<stdio.h>
int main(void)
{
    int decimal=0;
    scanf("%d",&decimal);
    printf("The hexadecimal equivalent is %x",decimal);
    return 0;
}
